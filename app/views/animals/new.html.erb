<h1>Add new animal to <%= @zoo.name %></h1>

<%= form_for [@zoo, @animal] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :photo %>
  <%= f.text_field :photo %>
  <%= f.submit "Submit" %>
<% end %>
